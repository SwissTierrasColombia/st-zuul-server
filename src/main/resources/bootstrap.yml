spring:
  application:
    name: st-zuul-server
  profiles:
    active: 
      - "develop"
      
---
spring:
  profiles: develop
  cloud:
    config:
      failFast: true
      uri: http://localhost:8889
      
---
spring:
  profiles: production
  cloud:
    config:
      failFast: true
      uri: http://st-config-server:8890